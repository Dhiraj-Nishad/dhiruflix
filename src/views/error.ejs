<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>404 - DhiruFlix</title>
    <style>
      body {
        margin: 0;
        font-family: Arial, sans-serif;
        background-color: #000;
        color: white;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
        background-image: url("/images/error-background.jpg"); /* Replace with your background image */
        background-size: cover;
        background-position: center;
        overflow-x: hidden;
      }

      header {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        background-color: #000;
        padding: 10px 20px;
        display: flex;
        align-items: center;
      }

      header h1 {
        color: #e50914;
        font-size: 24px;
        margin: 0;
      }

      .error-container {
        text-align: center;
        margin-top: 50px;
      }

      .error-container h1 {
        font-size: 48px;
        margin-bottom: 10px;
      }

      .error-container p {
        font-size: 18px;
        margin-bottom: 20px;
      }

      .error-container .error-code {
        font-size: 16px;
        color: #e50914;
        margin-top: 20px;
      }

      .error-container a {
        display: inline-block;
        padding: 10px 20px;
        background-color: white;
        color: #000;
        text-decoration: none;
        font-weight: bold;
        border-radius: 5px;
      }

      .error-container a:hover {
        background-color: #e50914;
        color: white;
      }

      footer {
        position: absolute;
        bottom: 10px;
        right: 20px;
        font-size: 12px;
        color: #b3b3b3;
      }
      footer .footer1 {
        display: flex;
        margin: 0 auto;
      }

      footer .footer1 div {
        border-right: 1px solid white;
      }
      footer p {
        color: #b3b3b3;
        margin: 0 auto;
      }
      header h1 a {
        color: #e50914;
        text-decoration: none;
      }
    </style>
  </head>
  <body>
    <!-- Header -->
    <header>
      <h1><a href="/">DhiruFlix</a></h1>
    </header>

    <!-- Error Content -->
    <div class="error-container">
      <h1>Lost your way?</h1>
      <p>
        Sorry, we can't find that page. You'll find lots to explore on the home
        page.
      </p>
      <a href="/">DhiruFlix Home</a>
      <p class="error-code">Error Code NSES-404</p>
    </div>
  </body>
</html>
